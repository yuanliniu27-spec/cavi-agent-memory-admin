# CAVI · Agent 记忆标签运营后台 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | CAVI · Agent 记忆标签运营后台 PRD |
| 版本 | v1.0（评审版） |
| 关联需求 | 《Agent记忆逻辑调整》（飞书 Wiki） |
| 交付形态 | 高保真交互原型 `cavi-agent-memory-admin.html` + 本文档 |
| 状态 | 待评审 |

---

## 1. 背景与目标

### 1.1 背景

CAVI 的 AI 购车顾问会持续沉淀用户画像：品牌偏好、车系兴趣、车身类型、预算段、首付区间、月供区间、能源类型等记忆标签。这些标签目前只存在于 Agent 会话链路内部，运营、增长与内容侧**无法查看、无法核对、无法按条件圈人**，导致三件事做不了：

1. 无法验证记忆写入是否正确（标签从哪来、什么时候写的、频次多少，全都不可见）；
2. 无法按标签维度圈定人群做定向运营（比如"预算 30 万 + 首付 25% + 偏好 SUV"的高意向金融用户）；
3. 无法把画像数据导出给投放、内容选题、金融定价等下游使用。

### 1.2 目标

搭建一个**面向内部运营的记忆标签查询后台**，实现：

- **可看**：按设备（device_id / uuid）实时查看单个用户的完整记忆标签画像与写入时间线；
- **可筛**：支持标识反查 + 环境维度 + 标签维度（两级下拉）组合筛选；
- **可导出**：按当前筛选结果导出 CSV / JSON，字段可控、默认脱敏、操作留痕。

### 1.3 成功指标（上线后 30 天）

| 指标 | 目标值 | 说明 |
| --- | --- | --- |
| 画像完整度（≥5 个标签组的用户占比） | ≥ 45% | 北望指标，衡量记忆写入链路健康度 |
| 标签可回溯率 | 100% | 任意标签均能定位到来源会话/浏览事件 |
| 运营侧圈人耗时 | 从"不可得"降至 < 2 分钟 | 从选条件到拿到导出文件 |
| 导出任务成功率 | ≥ 99% | 含异步大任务 |

---

## 2. 范围

### 2.1 本期范围（P0）

| 模块 | 内容 |
| --- | --- |
| 用户记忆查询 | 组合筛选 + 用户列表 + 画像详情抽屉 |
| 标签字典 | 8 类标签的取值口径、来源、触发规则、排序与上限 |
| 标签分布看板 | 预算/首付/月供/能源/品牌 Top10/画像完整度 聚合分布 |
| 导出 | CSV / JSON，字段可勾选，脱敏，留痕，异步大任务 |

### 2.2 暂不在本期范围（P1，保持占位）

- 设备-账号绑定审计页
- 导出任务记录页（明细列表）
- 系统设置页（维度开关、字典维护）
- 标签人工修正的审批流（本期先做直接修正 + 留痕）

### 2.3 明确不做

- 不做用户端任何可见变化（纯内部后台）；
- 不做画像的实时推送/订阅告警（后续单独评估）；
- 不做跨租户/多品牌隔离（当前单业务线）。

---

## 3. 用户与权限

| 角色 | 典型使用者 | 权限 |
| --- | --- | --- |
| 运营（只读） | 增长运营、内容运营 | 查询、查看画像、看板查看、导出（脱敏，单次 ≤ 1 万行） |
| 运营主管 | 运营负责人 | 上述 + 导出原始字段（需二次确认）、导出上限 5 万行 |
| 数据/后端 | 数据同学 | 上述 + 标签字典维护、修正记录查看 |
| 管理员 | 技术负责人 | 全权限 + 系统设置 |

**权限实现要点**：导出「是否脱敏」「导出上限」按角色在后端硬限制，前端仅做展示与提示，不可绕过。

---

## 4. 信息架构

```
CAVI 运营后台
├── 用户记忆查询（主功能）
│   ├── 筛选区（标识 / 环境 / 标签两级）
│   ├── 统计卡（4 张，随筛选联动）
│   ├── 用户列表（11 列）
│   └── 画像详情抽屉
├── 标签字典
├── 标签分布看板
├── 设备-账号绑定审计（P1）
├── 导出任务记录（P1）
└── 系统设置（P1）
```

---

## 5. 功能需求

### 5.1 组合筛选区

#### 5.1.1 标识反查（文本框）

支持输入任一标识模糊匹配，命中即返回：**device_id / uuid / 关联账号（usr-xxx）/ 手机号（脱敏后的尾号）/ 邮箱**。

- 输入即触发（无需点查询按钮），300ms 防抖；
- 匹配为不区分大小写的包含匹配。

#### 5.1.2 环境维度筛选

| 字段 | 控件 | 取值 |
| --- | --- | --- |
| 语言 | 下拉单选 | 全部 / Español / English / 中文 |
| 设备端 | 下拉单选 | **全部 / 移动 / PC**（仅两档，不细分 iOS/Android/浏览器） |
| 登录状态 | 下拉单选 | 全部 / 已登录 / 匿名或已退出 |
| 更新时间 | 日期区间 | 起止日期（YYYY-MM-DD） |

#### 5.1.3 标签维度筛选（核心交互，两级下拉）

**交互定义**：筛选区横向排列 7 个维度胶囊按钮，**先点维度 → 下拉展开该维度的具体标签值 → 勾选即生效**。

| 维度按钮 | 下拉可选值（示例，取自标签字典） |
| --- | --- |
| 品牌偏好 | Nissan / Toyota / Kia / Ford / Chevrolet / RAM / VW / Mazda / Honda / BYD / MG / Tesla …（主数据全量） |
| 车系兴趣 | Nueva Electra / Versa / Sentra / Seltos / Fortuner / F-150 / Silverado / Hilux / Seal / ZS EV / Dolphin / Corolla / Camry / CX-30 / Civic / Aveo / Model 3 / bZ4X … |
| 车身类型 | SUV / Sedan / Hatchback / Pickup / Coupé / Minivan / Van / Deportivo / Wagon（9 枚举） |
| 全款预算段 | $150,000–250,000 / $200,000–300,000 / $250,000–350,000 / $300,000–450,000 / $400,000–600,000 / $500,000–700,000 … |
| 首付占比区间 | [0-10%) / [10-15%) / [15-20%) / [20-25%) / [25-30%) / [30-40%) / [40-50%)（8 档枚举） |
| 月供区间 | < $5,000 / $5,000–10,000 / > $10,000 / 不限（4 档枚举） |
| 能源类型 | Gasolina / Híbrido / 油混 / Diésel / 纯电 / 插电混动（6 枚举） |

**筛选语义（必须严格实现）**：

- **维度内多选 = 或（OR）**：品牌勾 Nissan + Kia → 命中任一品牌即返回；
- **维度之间 = 且（AND）**：品牌 = Nissan 且 车身 = SUV → 两个维度都命中才返回；
- **未勾选任何值的维度不参与筛选**（即"不限"）；
- 空值语义：标签为「–」（未采集到）的用户，在勾选了该维度任意值时**不返回**。

**交互细节**：

- 已选值的维度按钮**高亮（品牌黄）+ 显示已选数量徽标**；
- 打开一个维度面板时自动收起其他面板；点击面板外部区域收起；
- 每个面板底部提供「清空该维度」；
- 顶部「重置」按钮清空全部筛选条件。

**分页与排序**：默认按「最近更新时间」倒序，每页 20 条，支持翻页与每页条数切换（20/50/100）。

### 5.2 统计卡（4 张，随筛选实时联动）

| 卡片 | 口径 |
| --- | --- |
| 24h 新增画像用户 | 最近 24 小时内首次写入任一记忆标签的 device_id 去重数 |
| 累计画像用户数 | 有至少 1 个记忆标签的 device_id 去重总数 |
| 平均用户标签数 | 当前筛选结果集的平均标签组数（满分 8） |
| 符合条件数 | 当前筛选条件命中的用户数 |

### 5.3 用户列表（11 列）

| # | 列名 | 展示规则 |
| --- | --- | --- |
| 1 | 设备标识 | device_id，等宽字体；下方小字展示关联账号或"匿名（未登录）" |
| 2 | UUID | 完整 uuid，等宽字体，支持复制 |
| 3 | 设备端 | 胶囊标签：移动 / PC |
| 4 | 语言 | 胶囊标签：Español / English / 中文 |
| 5 | 品牌偏好 Top3 | 最多 3 个品牌胶囊 |
| 6 | 车系兴趣 Top10 | 展示 Top10 全量；浏览行为命中项**带频次角标**（如 `Versa ×6`） |
| 7 | 预算偏好 | 预算段胶囊；无数据展示「–」 |
| 8 | 首付区间 | 首付档位胶囊；无数据展示「–」 |
| 9 | 能源偏好 | 能源类型胶囊（多类型全部展示） |
| 10 | 标签总数 | 形如 `7/8`，分母为 8 类标签组 |
| 11 | 最近更新时间 | 时间戳；24h 内更新附「24h 内更新」徽标 |

行点击 → 打开画像详情抽屉。空结果展示引导文案（提示放宽标签或时间条件）。

### 5.4 画像详情抽屉

点任意行从右侧滑出，内容分四段：

1. **身份与环境**：device_id、UUID、关联账号、手机号（脱敏）、语言、设备端、登录状态、更新时间；
2. **记忆标签全量（8 类）**：每类标签逐条展示 `值 + 来源（AI会话 / 浏览行为 / 系统 / 收入推导 / 车系反推）+ 首次/最近时间 + 频次 + 置信度`；
3. **写入时间线**：按时间倒序的事件流，可回放到具体会话或浏览行为，说明"这条偏好从哪来"；
4. **设备-账号绑定链路 + 人工修正入口**：展示当前绑定关系与迁移规则命中情况；运营可对单个标签做人工修正（记录修正人、时间、原值、新值）。

### 5.5 标签字典页

以表格形式列出 8 类标签的定义，字段包括：标签组 / 取值范围 / 来源 / 完整触发规则 / 排序与上限。

**必须写入文档的裁决结论**：

- 车系兴趣**整体按时间倒序**（纯会话表达的记录不带频次）；**仅浏览行为命中的车系附带频次**；
- 车系若命中多种能源类型，**全部记录并去重**（不做单值收敛）；
- 语言偏好优先级：**会话内显式要求 > 系统默认**（如中文会话要求覆盖西语默认）；
- 预算段 ±15% 落段规则、月供推导（月收入 15%–30%）作为 `source` 写入说明；
- 首付、月供区间类标签**全走枚举字典**，保证可筛选、可统计、可导出。

### 5.6 标签分布看板

以条形图聚合展示当前筛选结果集的分布：

- 全款预算段分布、首付占比区间分布、月供区间分布、能源类型分布、品牌偏好 Top10、画像完整度分布（另含设备端、语言构成）。

**业务用途**（需在看板页标注）：预算/首付/月供分布 → 喂金融定价与投放定向；品牌车系榜 → 喂内容选题与 SEO；画像完整度 → 作为北极星指标监控上报链路是否漏写。

### 5.7 导出

| 项 | 需求 |
| --- | --- |
| 格式 | CSV（UTF-8 BOM，Excel 直开）/ JSON |
| 范围 | 当前筛选条件的全量结果（非当前页） |
| 字段 | 按标签组勾选，默认全选；基础字段（device_id / uuid / 端 / 语言 / 更新时间）必选 |
| 脱敏 | **手机号默认脱敏**（形如 `+52 55 **87 4321`）；原始值导出需主管及以上角色二次确认 |
| 账号列 | 可选「是否展开关联账号」；对应需求中"匿名画像挂设备、登录迁移账号"的绑定规则 |
| 上限 | 单次 ≤ 5 万行；超出转**异步任务**，完成后站内通知 |
| 留痕 | 记录导出人、时间、筛选条件、字段集合、行数、脱敏状态，供审计 |
| 文件命名 | `cavi_memory_{yyyyMMdd_HHmm}_{条件摘要}.csv` |

---

## 6. 标签数据字典（8 类）

| # | 标签组 | 取值范围 | 写入来源 | 关键规则 |
| --- | --- | --- | --- | --- |
| 1 | 品牌偏好 | 品牌主数据，Top3 | AI 会话（语义关键词全量匹配） | 会话内品牌语义关键词命中即写，保留 Top3 |
| 2 | 车系兴趣 | 车系主数据，Top10 | AI 会话 + 浏览行为 | 整体时间倒序；浏览命中附频次；多能源全记去重 |
| 3 | 车身类型 | 9 枚举，Top3 | AI 会话 | 枚举归一，避免同义写法（sedán/Sedan） |
| 4 | 全款预算段 | 枚举区间 | AI 会话 | ±15% 落段，记录落段依据 |
| 5 | 首付占比区间 | 8 档枚举 | AI 会话 | 百分比归一为闭开区间 |
| 6 | 月供区间 | 4 档枚举 | AI 会话 / 收入推导 | 月收入 15%–30% 推导时 source 标记「收入推导」 |
| 7 | 能源类型 | 6 枚举，Top6 | AI 会话 / 车系反推 | 车系反推需标 source「车系反推」 |
| 8 | 身份与环境 | 语言 / 设备端 / 登录态 | 系统设置 + 会话覆盖 | 会话显式语言 > 系统默认语言 |

**标签实例存储结构**（每个标签值一条记录）：

```
{ tag_type, value, source, session_id, first_seen, last_seen, freq, confidence, rank }
```

---

## 7. 数据模型（后端落地参考）

### 7.1 三表模型

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| `memory_profile` | `device_id`(PK), `uuid`, `linked_accounts[]`, `lang`, `platform`, `login_state`, `created_at`, `updated_at` | 用户/设备主档 |
| `memory_tags` | `device_id`(FK), `tag_type`, `value`, `source`, `session_id`, `freq`, `first_seen`, `last_seen`, `rank`, `confidence`, `updated_by` | 标签实例，一个用户一类标签多条 |
| `tag_events` | `event_id`, `device_id`, `event_type`(会话/浏览/系统/修正), `session_id`, `payload`, `event_at` | 事件明细，供抽屉时间线回放与溯源 |

### 7.2 绑定规则（对应需求文档）

1. 匿名访问：画像挂 `device_id`；
2. 设备登录账号：画像迁移至账号，`device_id` 保留为关联设备；
3. 退出登录：此后新产生的画像仅绑定至下次无画像的新账号（不回流旧账号）。

---

## 8. 接口定义（查询服务）

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/memory/users` | GET | 参数：`ids`(标识反查)、`lang`、`platform`、`login_state`、`updated_from`、`updated_to`、`tags`(形如 `brand:Nissan,Kia;body:SUV`)、`page`、`size`。返回列表 + 统计卡数据 |
| `/admin/memory/users/{device_id}/realtime` | GET | 单用户实时画像：8 类标签 + 来源 + 时间线 + 绑定链路 |
| `/admin/memory/export` | POST | 提交导出任务（条件 + 字段集 + 脱敏开关），返回 `task_id`；大任务异步 |
| `/admin/memory/meta/dict` | GET | 标签字典（维度 → 具体值），供前端两级下拉渲染 |
| `/admin/memory/stats/distribution` | GET | 分布看板聚合数据 |

**筛选参数编码建议**：`tags` 用 `维度:值1,值2;维度:值3` 结构，与原型语义一致（维度间 AND、维度内 OR），后端一次 SQL 完成过滤，避免前端二次过滤。

---

## 9. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 列表首屏 P95 < 800ms；筛选联动 P95 < 500ms；实时画像接口 P95 < 1s |
| 数据时效 | 画像标签写入后 **≤ 5 分钟**在后台可见 |
| 安全合规 | 手机号/邮箱默认脱敏展示；导出留痕；后台仅内网/VPN 可达；中国团队访问墨西哥用户数据需符合当地数据合规要求，原始 PII 默认不可导 |
| 权限 | 见第 3 节，导出上限与脱敏开关由后端强制 |
| 可用性 | 只读后台，降级策略：查询服务异常时展示上次成功结果 + 明显提示，不阻塞导出任务提交 |
| 可观测 | 统计卡异常波动（如 24h 新增骤降 > 50%）需有监控告警 |

---

## 10. 验收标准

1. 输入 device_id / uuid / 手机号尾号 任一，均可反查到对应用户；
2. 标签筛选为两级交互：点维度 → 下拉出具体值 → 勾选生效；维度内 OR、维度间 AND 语义正确；
3. 列表 11 列字段齐全且与导出字段一致；车系兴趣展示 Top10，浏览命中项带频次角标；
4. 抽屉能完整展示 8 类标签，且每类标签可追溯到来源会话/浏览事件；
5. 导出：字段勾选生效、默认脱敏、> 5 万行转异步、留痕记录完整可查；
6. 标签字典页包含冲突裁决结论（车系时间倒序 + 浏览附频次、多能源全记去重）；
7. 分布看板 6 组图表随筛选条件联动刷新。

---

## 11. 里程碑

| 阶段 | 交付物 |
| --- | --- |
| M1 评审 | 本 PRD + 高保真原型（当前版本）评审通过，确认维度增减与导出字段 |
| M2 后端 | 三表模型落地 + 5 个接口联调完成，画像写入 → 后台可见 ≤ 5 分钟 |
| M3 前端 | 查询页 / 抽屉 / 导出全流程可用，字典页与看板页接入真实数据 |
| M4 灰度 | 运营与数据同学内测，导出留痕与权限校验通过 |
| M5 上线 | 正式发布，P1 三页（绑定审计 / 导出记录 / 系统设置）排期启动 |

---

## 12. 待确认问题

1. 导出上限 5 万行是否满足投放侧需求？是否需要支持定时/订阅式导出？
2. 标签人工修正是否需要审批流，还是直接生效 + 留痕即可？
3. 分布看板的统计口径是"当前筛选结果集"还是"全量用户"？本期按前者实现，如需两者切换请确认。
4. 车牌/车型等 PII 相邻字段是否也需要脱敏策略（当前仅手机号、邮箱）。
